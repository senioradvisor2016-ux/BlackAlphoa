name: macOS Swift Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run SwiftPM tests (AlphaJunoCore)
        shell: bash
        run: |
          cd AlphaJunoPG300
          swift test -v

